# Specs

Implementation-ready specs before code gets written. Sections to include: soul, user, principles, IA, files to change, verification gates.

The dev (Claude Code, Codex, or a human) reads the spec, then builds against it. The build does not start until the spec is committed.
